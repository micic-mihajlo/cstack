interface:
  display_name: "cstack"
  short_description: "Codex-native cstack-style engineering execution."
  default_prompt: "Use $cstack. Match the requested outcome, ground in the repository, make the smallest coherent change, forbid fake tests, mocks, and monkey-patching, and verify the real path."
policy:
  allow_implicit_invocation: true
